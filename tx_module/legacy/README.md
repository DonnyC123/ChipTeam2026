# Legacy TX Module Code

This directory keeps the previous multi-queue FIFO and scheduler code as reference material.

The current deliverable path is `../rtl/tx_subsystem_axis_1q.sv`; do not include files under this directory in the active FPGA source set unless the multi-queue architecture is intentionally restored.n